namespace Argus.Services
{
    public class SupportRequestService
    {
        /*Don't forget to ignore the global filter when receiving user requests to see the requests of dismissed (deleted) users:
        .Include(r => r.User.IgnoreQueryFilters())*/
    }
}
